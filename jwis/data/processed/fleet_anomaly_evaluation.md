# JWIS Fleet Anomaly Detector — Evaluation Report (Case 1)

_Auto-generated by `scripts/train_fleet_model.py`. Re-run to reproduce._

## Purpose

Detect out-of-corridor / anomalous waste-truck GPS behaviour (illegal detours, off-route stops, service-area breaches) for the Fleet Monitoring & Supervision case. Model: **Isolation Forest** (unsupervised novelty detection) on features `[latitude, longitude, speed_kmh]` — the exact contract consumed by `backend/app/engine.py::detect_route_deviation`.

## Data provenance (honest)

- **REAL geography:** 259 DKI mainland kelurahan centroids from `kelurahan_dki_full_267.geojson` are used as collection corridors.
- **REAL fleet scale:** DKI truck census (`data_truk_sampah_dki.csv`) totals **7081 units**, used to ground fleet size/mix.
- **SIMULATED pings:** per-truck GPS logs are not an open dataset, so normal operating points are sampled around real collection geography at realistic collection speeds, and a labelled anomaly set (off-corridor, abnormal speed, out-of-service-area) is generated to MEASURE detector quality. Swap in real AVL/GPS logs and re-run to retrain — the pipeline and feature contract are unchanged.

## Evaluation (labelled holdout)

- Train (normal only): **6,000** points
- Test: **2,000** normal + **1,000** labelled anomalies
- **Anomaly precision:** 0.806
- **Anomaly recall:** 0.664
- **Anomaly F1:** 0.728
- **Overall accuracy:** 0.835

## Reproducibility

- Command: `python scripts/train_fleet_model.py` from `jwis/`.
- Output: `backend/data/models/isolation_forest_fleet.joblib` (previous model backed up to `_synthetic_backup/`), and this report.
